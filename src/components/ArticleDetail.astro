---
import ArticleCover from './ArticleCover.astro';

interface Props {
	title: string;
	description?: string;
	date?: Date;
	tags?: string[];
	cover?: string;
	sectionLabel?: string;
	backHref?: string;
	backLabel?: string;
	footerHref?: string;
	footerLabel?: string;
}

const {
	title,
	description,
	date,
	tags,
	cover,
	sectionLabel = '知识库',
	backHref = '/articles/',
	backLabel = '返回亲戚常识',
	footerHref = '/articles/',
	footerLabel = '返回文章列表',
} = Astro.props;

function formatDate(value: Date | undefined) {
	if (!value) return '';
	return value.toLocaleDateString('zh-CN', {
		year: 'numeric',
		month: 'long',
		day: 'numeric',
	});
}

const hasMeta = date || (tags && tags.length > 0);
---

<div class="article-detail mx-auto w-full max-w-6xl">
	<a
		href={backHref}
		class="article-detail__back mb-6 inline-flex items-center gap-1.5 text-sm font-medium text-ink-muted no-underline transition hover:gap-2 hover:text-indigo dark:text-stone-400 dark:hover:text-stone-200"
	>
		<svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
			<path d="M19 12H5M12 19l-7-7 7-7" stroke-linecap="round" stroke-linejoin="round" />
		</svg>
		{backLabel}
	</a>

	{cover && (
		<div class="article-detail__cover mb-8 md:mb-10">
			<ArticleCover src={cover} alt={title} variant="hero" />
		</div>
	)}

	<header class="article-detail__header mb-8 md:mb-10">
		<p class="text-xs font-medium text-ink-muted dark:text-stone-500">{sectionLabel}</p>
		<h1 class="font-display mt-1 text-[clamp(1.5rem,2.5vw+0.8rem,2rem)] font-bold leading-snug tracking-tight text-ink dark:text-stone-100">
			{title}
		</h1>
		{
			description && (
				<p class="mt-3 text-base leading-relaxed text-ink-muted dark:text-stone-400">{description}</p>
			)
		}
		{
			hasMeta && (
				<div class="mt-4 flex flex-wrap items-center gap-2">
					{
						date && (
							<time
								class="text-xs text-ink-muted dark:text-stone-400"
								datetime={date.toISOString()}
							>
								{formatDate(date)}
							</time>
						)
					}
					{
						tags?.map((tag) => (
							<span class="qq-tag px-2.5 py-0.5 text-xs">{tag}</span>
						))
					}
				</div>
			)
		}
	</header>

	<div class="article-prose">
		<slot />
	</div>

	<footer class="mt-12 border-t border-stone-200/80 pt-8 dark:border-stone-800">
		<a href={footerHref} class="qq-link inline-flex items-center gap-1.5 text-sm font-medium">
			<svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
				<path d="M19 12H5M12 19l-7-7 7-7" stroke-linecap="round" stroke-linejoin="round" />
			</svg>
			{footerLabel}
		</a>
	</footer>
</div>
